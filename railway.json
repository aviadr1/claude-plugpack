{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting app...' && echo 'DATABASE_URL set:' ${DATABASE_URL:+yes} && echo 'Running alembic...' && alembic upgrade head && echo 'Alembic done, starting uvicorn...' && uvicorn plugpack.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
